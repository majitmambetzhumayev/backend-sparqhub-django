anthropic
authlib
celery
channels
channels-redis
daphne
django
django-cors-headers
django-encrypted-model-fields
django-storages[s3]
djangorestframework
djangorestframework-simplejwt
google-genai
httpx
# <2 pinned: mcp_server/server.py uses the 1.x FastMCP API, renamed/reshaped
# in 2.x (see mcp.server.mcpserver.MCPServer + the 2.x migration guide) --
# an unpinned install silently breaks mcp_server's tests the moment pip
# resolves a 2.x release, which is exactly what happened to CI between the
# 2026-07-27 green run and now.
mcp>=1.2.0,<2
mistralai
openai
# Pinned (unlike the rest of this file) -- an unpinned install resolves
# opentelemetry-semantic-conventions to a version newer than mistralai's own
# pin (<0.61,>=0.60b1), breaking `pip check`. 1.39.1 is the last release
# pair that stays inside that range.
opentelemetry-api==1.39.1
opentelemetry-sdk==1.39.1
opentelemetry-exporter-otlp-proto-http==1.39.1
pgvector
Pillow
psycopg2-binary
pypdf
python-decouple
python-docx
resend
sentry-sdk
uvicorn[standard]
whitenoise
